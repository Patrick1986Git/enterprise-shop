spring:
  datasource:
    hikari:
      maximum-pool-size: 4
      minimum-idle: 0
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  test:
    database:
      replace: none

security:
  jwt:
    secret: dGVzdC1vbmx5LTMyLWJ5dGUtand0LXNlY3JldC1rZXkh
    expiration: 3600000
    refresh-expiration: 86400000

stripe:
  api-key: sk_test_placeholder
  webhook-secret: whsec_placeholder
  public-key: pk_test_placeholder
